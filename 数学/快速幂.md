# 比较快的快速幂写法  
```c++
typedef long long ll;
const int mod =1e9+7;
ll powmod (ll a ,ll b)
{
    ll ret=1;
    for(;b;b>>=1)
    {
        if (b&1)ret=ret*a%mod;
        a=a*a%mod;
    }
    return ret;
}
```